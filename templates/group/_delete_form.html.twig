<form method="post" action="{{ path('app_group_delete', {'id': activeGroup.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить эту группу?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activeGroup.id) }}">
    <button class="badge badge-outline bg-red-200">Удалить группу</button>
</form>
