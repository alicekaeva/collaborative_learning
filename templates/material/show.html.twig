{% extends 'base.html.twig' %}

{% block title %}Material{% endblock %}

{% block body %}
    <h1>Material</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ material.name }}</td>
            </tr>
            <tr>
                <th>FileLink</th>
                <td>{{ material.fileLink }}</td>
            </tr>
            <tr>
                <th>MimeType</th>
                <td>{{ material.mimeType }}</td>
            </tr>
            <tr>
                <th>IsPrivate</th>
                <td>{{ material.isPrivate ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_material_index') }}">back to list</a>

    <a href="{{ path('app_material_edit', {'id': material.id}) }}">edit</a>

    {{ include('material/_delete_form.html.twig') }}
{% endblock %}
