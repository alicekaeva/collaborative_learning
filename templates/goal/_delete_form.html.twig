<form method="post" action="{{ path('app_goal_delete', {'id': goal.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить эту цель?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ goal.id) }}">
    <button><i class="fa-solid fa-trash-can"></i></button>
</form>
